# ── 시크릿 (절대 커밋 금지) ──
.env
.env.*
!.env.example
*.pem
*.key

# ── Python ──
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── Node / Frontend ──
node_modules/
dist/
build/
*.local

# ── DB / 런타임 데이터 ──
*.db
*.sqlite
*.sqlite3

# ── 로컬 설정 / OS ──
.claude/settings.local.json
.DS_Store
*.log

# ── 문서 (로컬 전용, 커밋 제외) ──
docs/
